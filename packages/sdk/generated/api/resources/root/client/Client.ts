/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as environments from "../../../../environments.js";
import * as core from "../../../../core/index.js";
import * as AirboltAPI from "../../../index.js";
import { mergeHeaders, mergeOnlyDefinedHeaders } from "../../../../core/headers.js";
import * as errors from "../../../../errors/index.js";

export declare namespace Root {
    export interface Options {
        environment?: core.Supplier<environments.AirboltAPIEnvironment | string>;
        /** Specify a custom URL to connect the client to. */
        baseUrl?: core.Supplier<string>;
        token?: core.Supplier<core.BearerToken | undefined>;
        /** Additional headers to include in requests. */
        headers?: Record<string, string | core.Supplier<string | undefined> | undefined>;
        fetcher?: core.FetchFunction;
    }

    export interface RequestOptions {
        /** The maximum time to wait for a response in seconds. */
        timeoutInSeconds?: number;
        /** The number of times to retry the request. Defaults to 2. */
        maxRetries?: number;
        /** A hook to abort the request. */
        abortSignal?: AbortSignal;
        /** Additional headers to include in the request. */
        headers?: Record<string, string | core.Supplier<string | undefined> | undefined>;
    }
}

export class Root {
    protected readonly _options: Root.Options;

    constructor(_options: Root.Options = {}) {
        this._options = _options;
    }

    /**
     * Returns a hello world message for API health check
     *
     * @param {Root.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link AirboltAPI.InternalServerError}
     *
     * @example
     *     await client.root.getWelcomeMessage()
     */
    public getWelcomeMessage(requestOptions?: Root.RequestOptions): core.HttpResponsePromise<AirboltAPI.GetResponse> {
        return core.HttpResponsePromise.fromPromise(this.__getWelcomeMessage(requestOptions));
    }

    private async __getWelcomeMessage(
        requestOptions?: Root.RequestOptions,
    ): Promise<core.WithRawResponse<AirboltAPI.GetResponse>> {
        const _response = await (this._options.fetcher ?? core.fetcher)({
            url:
                (await core.Supplier.get(this._options.baseUrl)) ??
                (await core.Supplier.get(this._options.environment)) ??
                environments.AirboltAPIEnvironment.Default,
            method: "GET",
            headers: mergeHeaders(
                this._options?.headers,
                mergeOnlyDefinedHeaders({ Authorization: await this._getAuthorizationHeader() }),
                requestOptions?.headers,
            ),
            timeoutMs: requestOptions?.timeoutInSeconds != null ? requestOptions.timeoutInSeconds * 1000 : 60000,
            maxRetries: requestOptions?.maxRetries,
            withCredentials: true,
            abortSignal: requestOptions?.abortSignal,
        });
        if (_response.ok) {
            return { data: _response.body as AirboltAPI.GetResponse, rawResponse: _response.rawResponse };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 500:
                    throw new AirboltAPI.InternalServerError(
                        _response.error.body as AirboltAPI.InternalServerErrorBody,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.AirboltAPIError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        switch (_response.error.reason) {
            case "non-json":
                throw new errors.AirboltAPIError({
                    statusCode: _response.error.statusCode,
                    body: _response.error.rawBody,
                    rawResponse: _response.rawResponse,
                });
            case "timeout":
                throw new errors.AirboltAPITimeoutError("Timeout exceeded when calling GET /.");
            case "unknown":
                throw new errors.AirboltAPIError({
                    message: _response.error.errorMessage,
                    rawResponse: _response.rawResponse,
                });
        }
    }

    protected async _getAuthorizationHeader(): Promise<string | undefined> {
        const bearer = await core.Supplier.get(this._options.token);
        if (bearer != null) {
            return `Bearer ${bearer}`;
        }

        return undefined;
    }
}
